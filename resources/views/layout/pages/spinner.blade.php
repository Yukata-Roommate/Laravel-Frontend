<div id="spinner" class="spinner-container">
    <div class="spinner-inner">
        <svg width="120" height="120" viewBox="-60 -60 120 120">
            <circle class="spinner" r="50" />
        </svg>

        <p class="spinner-text spinner-text-waiting">{{ __('yr-frontend::message.spinner.waiting') }}</p>
        <p class="spinner-text spinner-text-loading">{{ __('yr-frontend::message.spinner.loading') }}</p>
        <p class="spinner-text spinner-text-processing">{{ __('yr-frontend::message.spinner.processing') }}</p>
        <p class="spinner-text spinner-text-saving">{{ __('yr-frontend::message.spinner.saving') }}</p>
        <p class="spinner-text spinner-text-deleting">{{ __('yr-frontend::message.spinner.deleting') }}</p>
        <p class="spinner-text spinner-text-fetching">{{ __('yr-frontend::message.spinner.fetching') }}</p>
        <p class="spinner-text spinner-text-searching">{{ __('yr-frontend::message.spinner.searching') }}</p>
        <p class="spinner-text spinner-text-submitting">{{ __('yr-frontend::message.spinner.submitting') }}</p>
    </div>
</div>
